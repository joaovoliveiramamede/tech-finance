server:
    port: 8080
    servlet:
        encoding:
            force: true
            enabled: true
            charset: UTF-8

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/techfinance?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: false
        

logging:
  level:
    root: info
    '[com.techfinance.pessoal]': debug

jwt:
    secret: "minha-chave-super-secreta-com-32-caracteres"
    expiration: 3600000
