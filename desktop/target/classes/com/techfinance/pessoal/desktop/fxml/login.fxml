<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane xmlns:fx="http://javafx.com/fxml"
           fx:controller="com.techfinance.pessoal.desktop.controller.LoginController"
           styleClass="auth-root">

    <VBox alignment="CENTER">

        <VBox styleClass="auth-card" spacing="16">

            <padding>
                <Insets top="32" right="32" bottom="32" left="32"/>
            </padding>

            <Label text="Entrar"
                   styleClass="auth-title"/>

            <Label text="Acesse seu painel financeiro"
                   styleClass="auth-subtitle"/>

            <TextField fx:id="usernameField"
                       promptText="Usuário"
                       styleClass="auth-field"/>

            <PasswordField fx:id="passwordField"
                           promptText="Senha"
                           styleClass="auth-field"/>

            <Label fx:id="errorLabel"
                   visible="false"
                   managed="false"
                   styleClass="error-label"/>

            <Button text="Entrar"
                    maxWidth="Infinity"
                    onAction="#handleLogin"
                    styleClass="auth-button"/>

            <Button text="Criar conta"
                    maxWidth="Infinity"
                    onAction="#goToRegister"
                    styleClass="auth-link"/>

        </VBox>

    </VBox>

</StackPane>